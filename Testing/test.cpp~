#include <iostream>
#include <vector>

int main(int argc, char *argv[]) {
  std::cout << argc << std::endl;
  std::vector<int> my_vec = {5, 1, 3, 2, 4, 6};
  std::vector<int> my_vec2;
  my_vec.push_back(5);

  my_vec.push_back(5);
  my_vec.push_back(10);

  if (argc > 2) std::cout << argv[1] << std::endl;
  
  
  return 0;
}
